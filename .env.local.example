# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Supabase Project URL
# Get this from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (Public)
# Get this from: Supabase Dashboard → Settings → API → Project API keys → anon (public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Note: Keep your service_role key SECRET and never expose it to the client!
# Only use it in server-side code or API routes if needed.
